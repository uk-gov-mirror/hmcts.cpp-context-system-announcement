changelogFile: liquibase/systemannouncement-view-store-db-changelog.xml
liquibase.headless: true

# Liquibase 5 collects anonymous analytics by default; these jars run the migrations
# in the Helm pre-install job, so turn it off.
liquibase.analytics.enabled: false
